services:
  web:
    command: bun run dev --host 0.0.0.0 --port "$PORT"
    health: /
    portal:
      title: The Last Ember
      description: Development starter. The adventure is not implemented yet.
