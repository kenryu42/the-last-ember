services:
  web:
    command: bun run dev --host 0.0.0.0 --port "$PORT"
    health: /
    portal:
      title: The Last Ember
      description: A complete three-act solo deckbuilding adventure. Progress saves in this browser; export a journey from Settings to move it elsewhere.
